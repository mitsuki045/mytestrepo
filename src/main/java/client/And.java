package client;

public class And { 

  
  protected int newadd(int a, int b){
      int result = a * 2 + b / 3 - c * 4;
      result += (a + b) * (c - 2);
      result = result % 5 + (b * b) / (a + 1);
      return result;
  }
}

